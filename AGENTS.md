# Agent Instructions

The canonical schema for this knowledge base lives in [CLAUDE.md](CLAUDE.md). Read it in full
before doing anything here — it defines the three-layer architecture (raw / wiki / schema), the
directory layout, frontmatter conventions, and the workflows for `ingest`, `query`, `lint`,
`research`, and `schema` operations.

Any agent (Claude Code, Codex, OpenCode, …) working in this repo follows the same rules.
